BOOL __thiscall CMob::IsRisingByToss(CMob *this)
{
  IWzVector2D *m_pInterface; // ecx
  int v2; // eax
  BOOL result; // eax

  result = false; /*0x63a9ac*/
  if ( this->m_stat.nRiseByToss_ || this->m_bWaitingToBeSetTossed )
  {
    m_pInterface = this->m_pvc.m_pInterface; /*0x63a992*/
    v2 = m_pInterface ? (int)&m_pInterface[-3] : 0;
    if ( !*(_DWORD *)(v2 + 416) ) /*0x63a9a3*/
      return true; /*0x63a990*/
  }
  return result; /*0x63a9b1*/
}