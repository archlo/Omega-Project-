int __thiscall CMob::SetBallDestPoint(CMob *this, tagPOINT pt1, tagPOINT pt2, int bLeft, int nRange, int nRangea)
{
  int y; // ebx
  int v8; // esi
  int x; // edi
  int v10; // ecx
  double v11; // st7
  double v12; // st6
  long double v13; // st7
  int dy; // [esp+20h] [ebp+10h]
  int bLefta; // [esp+28h] [ebp+18h]

  y = pt2.y;
  if ( nRange )
    v8 = pt1.y - pt2.y;
  else
    v8 = pt2.y - pt1.y;
  x = bLeft;
  v10 = bLeft - pt2.x;
  bLefta = v8;
  dy = bLeft - pt2.x;
  if ( v8 <= 0 )
  {
    y = pt1.y - 1;
    if ( !nRange )
      y = pt1.y + 1;
    v8 = 1;
    v10 = 0;
    x = pt2.x;
    bLefta = 1;
    dy = 0;
  }
  v11 = (double)bLefta;
  v12 = DOUBLE_0_6;
  if ( DOUBLE_0_6 < fabs((double)dy / v11) )
  {
    if ( v10 <= 0 )
    {
      v11 = DOUBLE_0_6;
      v12 = (double)-v8;
    }
    x = (int)(v11 * v12 + (double)pt2.x);
    v10 = x - pt2.x;
  }
  v13 = (double)nRangea / sqrt((double)(v8 * v8 + v10 * v10));
  *(_DWORD *)pt1.x = (int)((double)(y - pt1.y) * v13 + (double)pt1.y);
  *(_DWORD *)(pt1.x + 4) = (int)(v13 * (double)(x - pt2.x) + (double)pt2.x);
  return pt1.x;
}
