void __thiscall CMob::ChangeAngerIndicator(CMob *this, int nIndicatorIndex)
{
  bool v2; // cc

  if ( nIndicatorIndex != this->m_nCurrIndicatorIndex )
  {
    v2 = nIndicatorIndex <= this->m_nGaugeCount;
    this->m_nAngerGaugeCount = 0;
    if ( v2 && nIndicatorIndex >= 0 )
      this->m_nCurrIndicatorIndex = nIndicatorIndex;
  }
}
