CRTTI __thiscall CMob::IsKindOf(CMob *this, const CRTTI *pRTTI)
{
  CRTTI result; // eax

  result.m_pPrev = &CMob::ms_RTTI_CMob;
  while ( result.m_pPrev != pRTTI )
  {
    result.m_pPrev = result.m_pPrev->m_pPrev;
    if ( !result.m_pPrev )
      return result;
  }
  return (CRTTI)1;
}
