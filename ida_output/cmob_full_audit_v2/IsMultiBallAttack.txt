int __thiscall CMob::IsMultiBallAttack(CMob *this, int nAction, int *nBulletNumber, int *nRange)
{
  MobAttackInfo *AttackInfo; // eax
  int v5; // ecx

  if ( (unsigned int)(nAction - 13) > 8 )
    return 0;
  AttackInfo = CMobTemplate::GetAttackInfo(this->m_pTemplate, nAction - 13);
  if ( !AttackInfo )
    return 0;
  if ( AttackInfo->nType != 2 )
    return 0;
  v5 = AttackInfo->nBulletNumber;
  if ( v5 <= 1 )
    return 0;
  *nBulletNumber = v5;
  *nRange = AttackInfo->tzRange.nRange;
  return 1;
}
