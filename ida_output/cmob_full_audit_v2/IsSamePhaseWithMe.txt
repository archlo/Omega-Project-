BOOL __thiscall CMob::IsSamePhaseWithMe(CMob *this)
{
  CStage *p; // esi
  ZRefCounted_vtbl *v3; // esi
  char *m_nPhase; // eax
  BOOL result; // eax

  p = g_pStage.p;
  result = false;
  if ( g_pStage.p )
  {
    if ( g_pStage.p->IsKindOf(&g_pStage.p->IUIMsgHandler, &CField::ms_RTTI_CField) )
    {
      if ( TSingleton<CUserLocal>::ms_pInstance.m_Data )
      {
        v3 = p[24].ZRefCounted::__vftable;
        if ( !v3 )
          return true;
        m_nPhase = (char *)this->m_nPhase;
        if ( (int)v3 >= (int)m_nPhase && m_nPhase == TSingleton<CUserLocal>::ms_pInstance.m_Data[983].m_str )
          return true;
      }
    }
  }
  return result;
}
