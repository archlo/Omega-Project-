void __thiscall CMob::OnEscortStopEndPermmision(CMob *this)
{
  CMob *v1; // ecx
  IWzVector2D *v2; // eax
  IWzVector2D *v3; // eax
  IWzVector2D *m_pInterface; // eax

  if ( CMob::IsActive(this) && (v2 = v1->m_pvcActive.m_pInterface) != nullptr && v2 != (IWzVector2D *)12 )
  {
    v3 = v2 - 3;
    v3[471].__vftable = nullptr;
    v3[472].__vftable = nullptr;
  }
  else
  {
    m_pInterface = v1->m_pvcActive.m_pInterface;
    if ( m_pInterface )
    {
      if ( m_pInterface != (IWzVector2D *)12 )
        CMob::ClearEscortInfo(v1);
    }
  }
}
