import { GamePanel } from './GamePanel.js';
import { WzSprite } from '../../render/WzSprite.js';
import { WzTextureLoader } from '../../render/WzTextureLoader.js';
import { WzPackage } from '../../wz/WzPackage.js';
import { WzAudioPlayer } from '../../render/WzAudioPlayer.js';
import { BuiltInFont } from '../BuiltInFont.js';
import type { DragTarget } from '../DragController.js';
export declare class VegaDialog extends GamePanel implements DragTarget {
    OnEnhance: ((equipItemTI: number, equipSlotPos: number, scrollItemTI: number, scrollSlotPos: number, whiteScrollUse: number, cashPos: number, cashItemId: number) => void) | null;
    OnClose: (() => void) | null;
    private _cashPos;
    private _cashItemId;
    private _equipItemTI;
    private _equipSlotPos;
    private _scrollItemTI;
    private _scrollSlotPos;
    private _whiteScrollUse;
    private _state;
    private _tStart;
    private _tEnd;
    private _gaugeProgress;
    private _resultCode;
    private _requestSent;
    private _background;
    private _btStart;
    private _btCancel;
    private _gaugeFill;
    private _gaugeFillContainer;
    private _arrow;
    private _countDigits;
    private _countContainer;
    private _successFrames;
    private _failFrames;
    private _twinklingFrames;
    private _spellingFrames;
    private _effectContainer;
    private _effectFrameIndex;
    private _effectFrameTimer;
    private _effectRunning;
    private _effectIsSuccess;
    private _spellingFrameIndex;
    private _spellingTimer;
    private _twinklingFrameIndex;
    private _twinklingStart;
    private _resultBg;
    private _resultText;
    private _allButtons;
    private _equipIcon;
    private _scrollIcon;
    private _equipLabel;
    private _scrollLabel;
    /** Item IDs that are valid scrolls (from VegaSpell numeric children). */
    private _scrollList;
    private _successSound;
    private _failSound;
    private _twinklingSound;
    private _audioPlayer;
    constructor(loader: WzTextureLoader, ui: WzPackage | null, font: BuiltInFont | null, soundPkg: WzPackage | null, audioPlayer: WzAudioPlayer | null);
    Open(cashPos?: number, cashItemId?: number): void;
    OnVegaResult(resultCode: number): void;
    setEquipIcon(sprite: WzSprite | null): void;
    setScrollIcon(sprite: WzSprite | null): void;
    /** Check whether `scrollId` is a valid scroll for this Vega dialog. */
    isRightScroll(scrollId: number): boolean;
    tryAcceptDrag(payload: unknown, _x: number, _y: number): boolean;
    update(_dt: number): void;
    handleMouseButton(x: number, y: number, down: boolean): boolean;
    onKeyPress(key: string): boolean;
    private _onEnhanceClick;
    private _onCancelClick;
    private _showResult;
    private _playEffect;
    private _showEffectFrame;
    /** EffectSpelling at (85, 114) repeating during gauge. */
    private _startSpellingAnimation;
    private _updateSpellingAnimation;
    private _stopSpellingAnimation;
    private _showSpellingFrame;
    private _showTwinklingFrame;
    /** EffectTwinkling at (85, 114) for ~2500ms on success. */
    private _startTwinklingAnimation;
    private _updateTwinklingAnimation;
    private _stopTwinklingAnimation;
    private _loadSounds;
    private _playSound;
    private _loadScrollList;
    private _updateGaugeGraphic;
    private _updateCount;
    private _showMessage;
    private _scheduleClose;
    private _loadCountDigits;
    private _loadEffectFrames;
    private _makeButton;
}
//# sourceMappingURL=VegaDialog.d.ts.map